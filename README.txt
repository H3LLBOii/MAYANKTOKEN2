
✅ RENDER DEPLOYMENT INSTRUCTIONS:

1. Go to https://dashboard.render.com/
2. Click "New Web Service"
3. Connect your GitHub repo (or upload manually)
4. Make sure these files exist:
   - app.py
   - templates/index.html
   - requirements.txt
   - render.yaml
5. Render will auto-detect Flask and deploy.
6. Open the URL and paste a fresh Facebook cookie.

💡 TIP: Always use a fresh cookie from browser DevTools (same day).
